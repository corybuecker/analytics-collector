{
  "name": "@corybuecker/analytics-collector",
  "version": "0.0.1-dev",
  "description": "Privacy-first web analytics aggregator with multiple storage integrations",
  "homepage": "https://github.com/corybuecker/analytics-collector#readme",
  "bugs": {
    "url": "https://github.com/corybuecker/analytics-collector/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/corybuecker/analytics-collector.git"
  },
  "license": "Unlicense",
  "author": "Cory Buecker",
  "type": "module",
  "scripts": {
    "build:watch": "nodemon --watch src --ext ts --exec \"npm run build\"",
    "build": "esbuild --bundle lib/index.js --outfile=dist/index.js --format=esm --sourcemap --minify",
    "prebuild": "rm -rf lib && rm -f dist/* && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "exports": {
    ".": "./dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  }
}